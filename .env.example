# Copy to `.env`. Leave these blank to run the app with the built-in mock
# identifier — no backend required. Fill them in to call the real
# identify-and-score Edge Function (see supabase/).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
