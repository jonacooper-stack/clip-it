// The welcome-screen hero background.
//
// This is a designed placeholder (a dusk wilderness scene) so the hero looks
// finished out of the box. To use a real wildlife photo, drop an image into
// assets/photos/ and point this require at it — e.g.
//   export const HERO_PHOTO = require('../../assets/photos/your-photo.jpg');
// Any image format Metro bundles (jpg / png / webp) works. The hero overlays a
// dark gradient for text legibility, so most photos look good with no tuning.
export const HERO_PHOTO = require('../../assets/photos/hero.png');
